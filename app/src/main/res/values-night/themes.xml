<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AuroraSpa" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorSecondaryContainer">@color/orange</item>

    </style>
    <style name="ThemeOverlay.AuroraSpa.MaterialDatePicker" parent="ThemeOverlay.Material3.MaterialCalendar">
        <!-- Màu nền chính -->
        <item name="colorSurface">@color/cream</item>
        <!-- Màu chữ -->
        <item name="colorOnSurface">@color/black</item>
        <!-- Màu nút chọn -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textAppearancePopupMenuHeader">@style/fontInter12Reg</item>
    </style>

</resources>